import numpy as np

#Create an array of lenght 5 with all elements as 0 (floats)
a = np.zeros(5)
print(a)

#Create an array of lenght 5 with all elements as 1 (floats)
b = np.ones(7)
print(b)

#Create an array of two dimensions (m,n) m rows and n columns
c = np.eye(3)
print(c)


#Force the type of array
d = np.ones(10, dtype=np.int64)
print(d)



